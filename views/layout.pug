html(lang="en")
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge, chrome=1")
    meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0")
    meta(name="google-site-verification" content="hbrD5JDsXxEOzDyn9p5raw2s1vuxAp1XTHVXh7NY5z0")
    title=title
    meta(name="description" content=description)
    meta(property="og:url" content=url)
    meta(property="og:title" content=title)
    meta(property="og:description" content=description)
    link(rel="canonical" href=url)
    link(rel="icon" href="/images/pf-logo.png" type="image/x-icon")
    link(href="//fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet")
    link(rel="stylesheet" href="//stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous")
    link(rel="stylesheet" href="/css/style.css?v=1")
    block head
  body
    .d-flex.flex-column(style=`${isApp ? 'height:100vh' : ''}`)
      include ./includes/nav
      .flex1
        block content
      if !isApp
        include ./includes/footer
    script(src="//code.jquery.com/jquery-3.2.1.min.js" integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4=" crossorigin="anonymous")
    script.
      $(function() {
        var threshold = 900,
            loaded = false,
            $window = $(window),
            $navLinks = $('ul.nav-links'),
            $hamburger = $('button.hamburger');
        var handleResize = function() {
          if($window.width() < threshold) {
            $hamburger.show();
            if(!loaded) {
              $navLinks.hide();
            }
          } else {
            $hamburger.hide();
            $navLinks.show();
          }
          loaded = true;
        };
        handleResize();
        $window.on('resize', handleResize);
        $hamburger.on('click', function() {
          $navLinks.slideToggle();
        });
      });
    block scripts